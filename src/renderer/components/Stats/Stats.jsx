import React from 'react';
import { hot } from 'react-hot-loader/root';

const stats = () => {
    return (
        <div className="container">
            <h2 className="container-title">Statistics</h2>
        </div>
    );
};

export default hot(stats);
