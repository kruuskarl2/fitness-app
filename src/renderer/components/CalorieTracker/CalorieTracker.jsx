import React from 'react';
import { hot } from 'react-hot-loader/root';

const calorieTracker = () => {
    return (
        <div className="container">
            <h2 className="container-title">Calorie tracker</h2>
        </div>
    );
};

export default hot(calorieTracker);
